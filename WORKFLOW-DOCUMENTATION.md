# 📋 سير العمل المنطقي لـ Taswiyah Hub

> تم إنشاء هذا الدليل لشرح كيفية عمل نظام المطابقة بشكل صحيح

---

## 🔄 **مراحل سير العمل:**

### **المرحلة 1️⃣: المنتظر (Pending)**
```
┌─────────────────────────┐
│   الفواتير والحوالات   │
│      المنتظرة          │
│   (بدون مطابقة)        │
└─────────────────────────┘
        ↓
   يتم إدارتها بـ:
   - الحوالات من البنك
   - الفواتير المبيعات
   - المستندات الأخرى
```

**الأفعال المتاحة:**
- ✅ إضافة حوالة جديدة
- ✅ إضافة فاتورة جديدة
- ✅ البحث والتصفية
- ✅ الانتقال إلى "مساعد المطابقة"

---

### **المرحلة 2️⃣: مساعد المطابقة (Assisted Matching)**
```
┌──────────────────────────────┐
│   المطابقة الآلية            │
│   (خوارزمية ذكية)            │
│                              │
│  تبحث عن:                    │
│  - نفس المبلغ                 │
│  - التواريخ القريبة           │
│  - نفس المرجع                 │
└──────────────────────────────┘
        ↓
   النتائج:
   ✓ مطابقات عالية الثقة
   ? مطابقات تحتاج مراجعة
   ✗ عدم وجود مطابقة
```

**الخوارزمية:**
```javascript
لكل حوالة:
  لكل فاتورة:
    إذا (المبلغ متطابق) و (التاريخ قريب):
      → إنشاء مطابقة
      → إضافة إلى قائمة مساعد المطابقة
      → الانتقال للحوالة التالية
```

**الأفعال المتاحة:**
- ✅ الموافقة على المطابقة (تأكيد)
- ✅ رفض المطابقة (إلغاء)
- ✅ نقل إلى "المطابقة اليدوية" للمراجعة
- ✅ العرض والتفاصيل

---

### **المرحلة 3️⃣: المطابقة اليدوية (Manual Matching)**
```
┌──────────────────────────────┐
│   المطابقة اليدوية           │
│   (اختيار يدوي)              │
│                              │
│  الحالات:                    │
│  1. المطابقات المرفوضة       │
│  2. المطابقات المعقدة        │
│  3. مطابقات من الصفر         │
└──────────────────────────────┘
        ↓
   المستخدم يختار:
   - حوالة من المنتظر
   - فاتورة من المنتظر
   - يتحقق من التطابق
   - يضغط: تأكيد المطابقة
```

**عملية المطابقة:**
```
1. اختيار الحوالة
   ↓
2. اختيار الفاتورة
   ↓
3. التحقق من البيانات
   - المبلغ
   - التاريخ
   - المرجع
   ↓
4. الضغط على: [تأكيد المطابقة ✅]
```

**الأفعال المتاحة:**
- ✅ مطابقة حوالة مع فاتورة
- ✅ مطابقة متعددة (حوالة + عدة فواتير)
- ✅ إلغاء المطابقة
- ✅ تصحيح البيانات

---

### **المرحلة 4️⃣: تأكيد المطابقة (Confirmation) ⭐**

> **هذه هي الخطوة الحاسمة!** عندما تضغط [تأكيد]، يحدث التالي:

```
[عند الضغط على: تأكيد المطابقة ✅]
        ↓
┌─────────────────────────────────────────────┐
│  الخطوة 1: نقل من المنتظر → المؤكد         │
├─────────────────────────────────────────────┤
│  الحوالة:                                   │
│  ❌ حذفها من: قائمة المنتظر                │
│  ✅ نقلها إلى: قائمة المؤكد                 │
│     (مع تحديث الحالة: confirmed)            │
│                                             │
│  الفاتورة:                                  │
│  ❌ حذفها من: قائمة المنتظر                │
│  ✅ نقلها إلى: قائمة المؤكد                 │
│     (مع تحديث الحالة: confirmed)            │
└─────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────┐
│  الخطوة 2: حذف من قوائم المطابقة           │
├─────────────────────────────────────────────┤
│  ❌ حذفها من: مساعد المطابقة               │
│  ❌ حذفها من: المطابقة اليدوية             │
│     (لن تظهر مجددًا)                        │
└─────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────┐
│  الخطوة 3: تسجيل المطابقة                 │
├─────────────────────────────────────────────┤
│  تسجيل كامل للمطابقة في السجل:             │
│  - ID المطابقة                              │
│  - ID الحوالة                               │
│  - ID الفاتورة                              │
│  - المبلغ                                   │
│  - تاريخ التأكيد                             │
│  - المستخدم الذي أكد                       │
│  - نوع المطابقة (آلي/يدوي)                  │
│  - الحالة: confirmed                        │
└─────────────────────────────────────────────┘
        ↓
       ✅ تمام! انتهت المطابقة
       لن تظهر هذه الحوالة والفاتورة مجددًا
```

---

### **المرحلة 5️⃣: المؤكد (Confirmed)**
```
┌────────────────────────────────────┐
│   المطابقات المؤكدة والنهائية    │
│   (لا يمكن تعديلها)               │
│                                    │
│  بيانات:                          │
│  ✓ حوالات مطابقة                  │
│  ✓ فواتير مطابقة                  │
│  ✓ تاريخ المطابقة                 │
│  ✓ المستخدم الذي أكد              │
└────────────────────────────────────┘
```

**الأفعال المتاحة:**
- 📖 عرض التفاصيل
- 📊 تقارير
- 🔍 بحث
- ❌ لا يمكن تعديل أو حذف (للأمان)

---

## 📊 **مثال عملي:**

### **السيناريو:**
```
لديك:
- حوالة من البنك: 1000$ (تاريخ: 2024-01-15)
- فاتورة مبيعات: 1000$ (تاريخ: 2024-01-14)

الخطوات:
```

**الخطوة 1: إضافة إلى المنتظر**
```
GET /api/pending
→ تظهر الحوالة والفاتورة معًا
```

**الخطوة 2: تشغيل المطابقة الآلية**
```
POST /api/matching/auto
→ يجد أن الحوالة والفاتورة متطابقتان
→ ينقلهما إلى "مساعد المطابقة"
```

**الخطوة 3: مراجعة مساعد المطابقة**
```
GET /api/matching/assisted
→ يظهر المطابقة الآلية
→ تضغط: [تأكيد ✅]
```

**الخطوة 4: تأكيد المطابقة (الخطوة الحاسمة!)**
```
POST /api/matching/confirm/match-123
{ "userId": 1 }

← تحدث التالي تلقائيًا:
  1. الحوالة والفاتورة تُنقل من "المنتظر" → "المؤكد"
  2. تُحذف من "مساعد المطابقة"
  3. يُسجل في سجل المطابقات
  4. الحالة تصبح: confirmed
```

**الخطوة 5: التحقق**
```
GET /api/pending
→ ✓ الحوالة والفاتورة اختفتا (انتقلتا إلى المؤكد)

GET /api/matching/assisted
→ ✓ المطابقة اختفت (تم تأكيدها)

GET /api/confirmed
→ ✓ الحوالة والفاتورة موجودتان (مع حالة: confirmed)

GET /api/reports/matches
→ ✓ المطابقة موجودة في السجل
```

---

## 🚫 **ما يجب أن لا يحدث:**

❌ **المشكلة القديمة:**
```
بعد المطابقة اليدوية:
- الحوالة والفاتورة لا تزال في "المنتظر" ❌
- المطابقة لا تزال في "المطابقة اليدوية" ❌
- تحتاج إلى مطابقتها مرة أخرى ❌
```

✅ **الحل:**
```
بعد الضغط على [تأكيد المطابقة]:
- الحوالة تنتقل: المنتظر → المؤكد ✅
- الفاتورة تنتقل: المنتظر → المؤكد ✅
- المطابقة تُحذف من: مساعد/يدوي ✅
- تسجيل شامل في السجل ✅
```

---

## 🔌 **API Endpoints الكاملة:**

### **1️⃣ المنتظر:**
```bash
# إضافة حوالة أو فاتورة
POST /api/transactions/add
{
  "type": "transfer",      # أو "invoice"
  "amount": 1000,
  "date": "2024-01-15",
  "reference": "TRF-123",
  "description": "حوالة بنكية"
}

# الحصول على قائمة المنتظر
GET /api/pending
```

### **2️⃣ مساعد المطابقة:**
```bash
# تشغيل المطابقة الآلية
POST /api/matching/auto

# الحصول على المطابقات
GET /api/matching/assisted
```

### **3️⃣ المطابقة اليدوية:**
```bash
# إجراء مطابقة يدوية
POST /api/matching/manual
{
  "transferId": "trans-123",
  "invoiceId": "inv-456",
  "userId": 1
}

# الحصول على قائمة المطابقة اليدوية
GET /api/matching/manual
```

### **4️⃣ تأكيد المطابقة (الخطوة الحاسمة!) ⭐:**
```bash
# تأكيد المطابقة والنقل إلى المؤكد
POST /api/matching/confirm/match-123
{
  "userId": 1
}

# الحصول على المؤكد
GET /api/confirmed
```

### **5️⃣ التقارير:**
```bash
# تقرير الحالة الشامل
GET /api/reports/status

# سجل جميع المطابقات
GET /api/reports/matches
```

---

## 📈 **تقرير الحالة الشامل:**

```json
GET /api/reports/status

{
  "success": true,
  "report": {
    "pending": {
      "count": 5,
      "total": 5000
    },
    "assisted": {
      "count": 3,
      "total": 3000
    },
    "manual": {
      "count": 2,
      "total": 2000
    },
    "confirmed": {
      "count": 15,
      "total": 15000
    },
    "matchRecords": 15
  }
}
```

---

## ✅ **الخلاصة:**

النظام الآن يعمل بمنطق صحيح:

1. **المنتظر** → قوائم الحوالات والفواتير الأولية
2. **مساعد المطابقة** → مطابقات آلية للمراجعة
3. **المطابقة اليدوية** → اختيار يدوي للتطابق
4. **تأكيد المطابقة** → النقل النهائي والتسجيل
5. **المؤكد** → السجل النهائي (لا يتكرر)

🎉 **لن تحتاج إلى مطابقة نفس الشيء مرتين!**
